apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  APP_ENV: "production"
  APP_NAME: "myapp"
  DATABASE_HOST: "postgresql-service"
  DATABASE_PORT: "5432"
  LOG_LEVEL: "info"
